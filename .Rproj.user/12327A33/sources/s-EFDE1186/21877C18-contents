# library calls
library(rvest)
library(tidyverse)

# url from indeed, other urls that may more valueble? ... needing to check

url <- 'https://www.indeed.com/cmp/Ryder/reviews?fcountry=ALL&fjobtitle=Driver&start=700'

webpage <- read_html(url)

# Probably better to take the route of accessing children to get the review ids?
container <- html_nodes(webpage,'div.cmp-review-container') %>% html_children()

# rating <- html_nodes(webpage,'div.cmp-ratingNumber') %>% html_text()
# title <- html_nodes(webpage,'.cmp-review-title') %>% html_text()
# location <- html_nodes(webpage,'.cmp-reviewer-job-location') %>% html_text()
# pros <- html_nodes(webpage,'.cmp-review-pro-text') %>% html_attr('data-reviewid')
# 
# html_nodes(webpage,'.cmp-review') %>% html_attrs()

